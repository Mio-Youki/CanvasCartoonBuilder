# 工具更新日志（tools/CHANGELOG）

> 独立于游戏本体的 CHANGELOG。工具版本独立演进，与游戏版本号无关。

## v2.7 补五 —— 修复增删场景后素材/FX 丢失 + 过渡未跟随场景边界
- **① 修复增删场景后素材丢失/FX 消失**：`syncSceneArrays` 只同步"纯数值/字符串数组"，**show 窗口数组与 fx.segs（嵌套数组）未随场景增删调整**——新增场景后元素 `show` 缺槽 → `elShown` 判隐藏 → 图层栏素材"丢失"；FX segs 缺段（面板兜底补但数据不完整）。新增 `fixWin`：show 增场景补 `[[0,1]]`（新场景默认可见）、删场景按索引移除；fx.segs 增场景沿用末段配置、删场景移除槽
- **② 修复过渡未跟随场景边界**：`applyTransition` 用 `edge = local % (LOOPv()/n)`（**假设每段等长**）——工具拖拽 sceneBorders 后各段不等长，过渡仍在"等分时长"触发 → 错位。改为用 `sceneBounds(part)` 计算**段内相对位置**（按真实 sceneBorders 起点），拖边界后过渡对齐当前场景起点
- **验证**：A1-A4（新增场景 show 补可见/删场景槽移除/卡片/FX 面板）PASS；T1-T3（非等分边界 [6,18,36] 下 scene2@18s/scene3@36s/scene0@0s 过渡在正确起点触发）PASS；`npm test` + typecheck 全绿
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.7 补四 —— 修复 FX 采样数无效 + 过渡按场景独立
- **① 修复 FX 采样数无效**：`applyPixelFilter` 早退条件只检查 palette/hue/brightness/contrast/saturation——**纯降采样（仅 pixelDiv>1 无调色）被提前 return**，采样数不生效；补 `pixelDiv` 进早退判断 → 纯降采样进入"缩小→放大"颗粒感路径（元素卡片/图片导入两处采样数本就有效，仅 FX 处受影响）
- **② 过渡按场景独立**：`transition`/`transitionDur` 改为**按场景数组**（每场景独立值，同一场景的分段共享）——工具端过渡行读/写当前场景槽，home-scene `applyTransition` 按 `scene(t)` 取当前场景值；旧单值兼容
- **验证**：纯 pixelDiv=4 块内同色 PASS；scene0 scan / scene1 fade 按场景独立 PASS；`npm test` + typecheck 全绿
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.7 补三 —— FX 按时段段组织（segs）+ 采样数统一 + A 档参数同行 + [S] 面板修复
- **① FX 数据模型重构（segs）**：从"按 fx 项分散管理"（crt/glitch 各自独立时段）改为 **按时段段组织**——`CFG.fx.segs[scene] = [{f:[f0,f1], crt, glitch, ..., palette, hue, brightness, contrast, saturation, pixelDiv}, ...]`，**每段独立存全套 A+B 参数**；去掉每项独立的「时段」按钮，改为 FX 轨直接分段（点击段=选中并在面板编辑该段全套参数）；home-scene `fxOn`/`fxVal` 按 t 找当前段读配置，旧模型（顶层按项）向后兼容；过渡 transition 仍场景级（不进时段段）
- **② FX 轨升级为分段条**：`#tl-fx` 显示各场景时段段色块（启用项越多越亮），点击段 = 选中 + 跳场景（面板同步）；拖端点共用缩放 / 双击切分（继承原段配置）/ 段尾 × 删除并入左边
- **③ 采样数统一**：三处（元素卡片 `pixelDiv` / 图片导入 `divisor` / FX 面板 `pixelDiv`）从下拉改为**整数输入框**（min=1 步长 1 自由键入）；FX 面板采样数移到**色板选择上方**
- **④ A 档参数同行**：crtOpacity/crtSpacing、noiseAlpha/noiseFrames、vignetteStrength 移到**对应开关左侧同行**
- **⑤ [S] 面板窗口条空白修复**：`buildBar` 首次构建后未调 `draw()` → **打开面板窗口条为 0 段**（数据实际存在但显示丢失，再度打开仍空白）；补首次 `draw()` 调用——打开即显示分段
- **验证**：home-scene segs 分段渲染（段0 nes 色板/段1 无，同场景分时切换）PASS + 过渡场景级 PASS；工具端 V1-V5（分段条/选中段参数/双击切分/采样数位置/A 档同行）PASS；[S] 面板 S1-S4（打开/关闭/重开/数据）PASS；**默认配置（无 fx）新旧渲染 DIFF=0**
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.7 补二 —— FX 增强：噪点多帧 + 调色参数 + 降采样前置 + 色板统一 + 场景内时段编辑
- **① 噪点随机性**：单张噪点 → **N 帧独立噪点轮换**（`noiseFrames` 默认 3，1~8 可调）——12fps 下每帧换帧 + 确定性小幅平移（雪花闪烁感）；`noiseAlpha` 强度可调
- **② 调色参数（零额外开销）**：`brightness`（-100~100）/`contrast`（0~3）/`saturation`（0~2）——**与 hue 色相旋转级联合并为单个 3×3 矩阵**（先调色再色相），每像素仍 9 次乘加；修复亮度偏移量纲（此前 /100 导致 +50 亮度几乎无效）
- **③ 降采样前置**：管线改为 **降采样(pixelDiv 1/2/4) → 调色矩阵 → LUT 色板 → 放大 → A 叠加 → 过渡**——调色只处理缩小后像素（开销 ÷ div²），放大后叠加层仍全分辨率；`pixelDiv` 默认 1（不降采样，全分辨率清晰）；修复此前"¼ 降采样"作为性能手段混淆了风格与性能
- **④ 色板统一**：FX 色板与素材导入减色**共用同一份定义** `pico8 32色 / nes 16色 / vga 16色 / gb 4色` + **通用 LUT 构建**（`paletteLut` 缓存，素材减色逐像素查表替代最近色距离，更快且结果一致）；home-scene 端 `FX_PALETTES`/`buildLut` 同值
- **⑤ 可调叠加纹理**：暗角 `vignetteStrength`（0~1）、CRT `crtOpacity`/`crtSpacing`、噪点 `noiseAlpha`/`noiseFrames`
- **⑥ 场景内时段编辑**：FX 面板每个开关行新增「时段」按钮 → **场景内分段编辑弹窗**（复用 show 窗口条交互：点击段内切分 / 空白加段 / 拖端点缩放共用 / 拖段移动 / **双击删除并入左边**；场景 chip 切换编辑场景）——数据 = `fx.<key>[scene] = [[f0,f1],…]` 窗口数组，home-scene `fxOn` 已支持零改动
- **像素级验证**：亮度+50 → (255,50,50)、饱和度0 三块灰度、对比度2、PICO-8 (255,0,77)、GB (139,172,15)、pixelDiv=2 像素块、噪点多帧轮换、暗角强度——全 PASS；**默认配置（无 fx）新旧渲染 DIFF=0**
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.7 补 —— FX 面板移入时间轴下方 + 修复下拉/开关/跨场景更新
- **① FX 面板 + 背景行移至时间轴下方**：从右侧图层栏（cfgform）移到 **canvas 时间轴下方的 `#tl-bar` 容器**（时间轴 FX 轨之下）——`buildLayerList` 不再向 cfgform 渲染背景/FX，改为渲染到 #tl-bar；右侧栏只留元素卡片；装配模式背景行同样显示在时间轴下方
- **② 修复下拉无选项**：色板（none/nes/gb）与过渡（fade/scan/wipe）下拉创建 option 后**未 `select.appendChild(o)`** → 下拉恒空；已补
- **③ 修复开关无法取消**：checkbox 值此前用 `!!fx[key]`——当值为**按场景数组**时数组非空恒 true → 永远勾选、无法取消；改为 `fxOnTool(key)`（取当前场景值），change 时正确写当前场景槽（常量→转数组，其余场景保持）
- **④ 修复跨场景面板不更新**：同上——`!!fx[key]` 数组恒 true 导致开关不随场景变化；修复后切场景重建，开关/下拉/色相显示当前场景值（buildLayerList 每帧由 tlScene 归位）
- **验证**：4 项 UI 测试 PASS——#tl-bar 含背景+滤镜FX、下拉有选项、scene0→scene1 开关 false→true、开关取消 true→false

## v2.7 —— 后处理 FX（CRT/故障/暗角/噪点 + NES/GB 色板 + 色相偏移 + 场景过渡）
- **全局滤镜配置 `CFG.fx`**：默认无字段 = 全关零开销；每项支持常量 / 按场景数组 / 场景内窗口数组 `[[f0,f1],…]`（与 show 同形态，复用 `val()`/`sceneBounds` 窗口判定）
- **A 档叠加层（预生成纹理，逐帧 drawImage）**：`crt`（半透明黑横线）/ `vignette`（径向暗角）/ `noise`（噪点逐帧平移，低 alpha）——纹理在 resize/palette/hue 变化时重建缓存；`glitch`（确定性随机水平位移条，随时间种子变化）
- **B 档像素滤镜（¼ 降采样 + 缓存）**：`palette` **NES 16 色常用子集 / GB 4 色**（65536 项 LUT 查表，构建一次缓存）；`hue` 色相偏移（3×3 线性矩阵，无逐像素三角函数）——先缩 ¼ 再滤镜再放大（像素风抗模糊，代价减半）
- **场景过渡 `transition`**：升级原硬编码暗场——`fade`（暗场闪切，默认=现状视觉不变）/ `scan`（扫描线）/ `wipe`（擦除）+ `transitionDur`（0.05~1s 可配）
- **时间轴 FX 轨（#tl-fx）**：时间轴下方新增滤镜时段色块行（crt/glitch/vignette/noise/palette/hue 各一行按场景着色），播放头同步；图层栏顶部新增「滤镜 FX」折叠面板（开关 + 色板下拉 + 色相输入 + 过渡下拉/时长）
- **渲染归属**：fx 只在 home-scene 渲染（工具预览经 sceneParamsMode 注入 HOME_SCENE 自动生效，游戏兼容）；装配模式不渲染 fx
- **像素级验证**：8 项 PASS——NES 红 (252,60,48) / GB 转绿 (139,172,15) / 色相 120° 红转绿 / CRT 黑线 / 暗角中心亮于角 / fade 暗场 / scan 上暗下亮 / glitch 无异常；**默认配置（无 fx）新旧渲染 DIFF=0**
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.6 补四 —— scroll 语义统一（angle 完整方向）+ 窗口起点相位归零 + [重复] 开关
- **① 方向语义统一（修复斜角平铺 y 轴对称）**：home-scene `scrollOffsets` 移除 `dir` 对 x 的符号修正，改为与工具端完全一致的 `{x: off×cos(angle), y: off×sin(angle)}`——`angle` 完整定义方向（0=右，90=下，180=左，270=上），**本体位移与副本排列沿同一方向连成线**（此前默认 dir=-1 使本体向左、副本向右 → 斜角时恰好 y 轴对称；90° 倍数因 cos=0 不受影响）。旧 `dir` 字段仅兼容：无 angle 时 left→180°/right→0°
- **② 斜线滚动两端同步**：`drawPartsRaw` line 分支终点 y 用 `y0`（元素原点）→ 改 `Y + pa.yOff`（含滚动/动画位移）——斜线滚动不再被拉伸变形（工具端本就正确，两端对齐）
- **③ 斜角轨迹角度修正（去掉 y 双加速）**：parts 瓦片循环 `blit(j×span×cosA, so.y + j×span×sinA)` → `blit(j×span×cosA, j×span×sinA)`——`so.y` 已并入 `Y0`，yAdd 再含一次会双加（45° 时 y 是 x 的两倍速，轨迹角度被拉偏）；单本体 `blit(0, so.y)` → `blit(0, 0)` 同理
- **④ 窗口起点相位归零（scrollWindowStart）**：滚动相位从**元素当前显示窗口起点**起算——`off = wrap((t - t0) × speed, span)`（t0 = 当前窗口起点）；元素在**每个 show 窗口（单段/多段）的最初以配置坐标 (x,y) 出现**（相位=0），随后沿 angle 运动；无 show 元素 t0=0（行为不变）。工具端复用 `showRanges`/`tlSceneRange` 同语义，装配模式 t0=0
- **⑤ [重复] 开关（scroll.repeat）**：imgForm「平铺周期」上方新增开关——**开（默认，undefined=开）= 生成副本平铺**（span=副本间距+相位周期）；**关（repeat:false）= 单本体**沿 angle 往返（span=往返距离，不生成副本）；渲染端 `repeat !== false` 判断
- **像素级验证**：6 项 PASS（45°/135° 方向语义、斜线两端同步、45° 无 y 双加速、窗口起点归零、repeat:false 单本体、repeat 默认平铺）；home-scene 侧 S1/S1b/S3/S4 同语义验证 PASS；**默认配置（无 scroll/无 show）新旧渲染 DIFF=0**
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.6 补三 —— 程序元素四角缩放 + 矢量缩放中间态 + Ctrl+点击图元多选 + part 多选青框群组
- **① 程序元素四角缩放修复**：黄框选中程序元素（selProg）时四角拖拽此前**完全失效**（只走整体移动）——pointerdown 程序元素分支缺 `hitCorner` 命中；新增 `presize` 拖拽（锚定对角缩放 parts 坐标，与矢量元素同语义，不写 e.w/h）
- **② 矢量元素四角缩放中间态修复**：拖动缩放时**只记录起始/最终状态、无中间态**——根因是 resize 处理器 `e.scroll.span = e.w` 在**无 scroll 字段的矢量元素**（commitDrawShape 新建）上抛 TypeError，`renderStage()` 未执行；加 `if (e.scroll)` 守卫 → 拖拽过程实时重绘
- **③ 命中容差（四角缩放失效的深层根因）**：`hitCorner` 边界判定 `p.x <= box.x+box.w` 遇 `stagePos` 除法浮点误差（如 26.000000000000001 ≤ 26 为 false）→ 右下角/下边角点击永远不命中 → 缩放失效/不流畅；`hitCorner` 与元素/程序元素框内命中统一加 **±0.5px 容差**
- **④ Ctrl+点击图元多选**：**[编辑] 模式 Ctrl+点击图元 = 加入/移出 partMulti**（与 Ctrl+A 全选同一状态，`animTarget` 批量动画同步生效）；此前 Ctrl+点击只单选
- **⑤ part 多选最外层青框（群组操作）**：partMulti 在逐图元青框之外**新增联合青框**（与单选一致的群组手柄）——**四角等比缩放**（`pmresize`，锚定对角缩放各 part 坐标）、**整体移动**（`pmmove`）、**旋转 ●**（`pmrot`，各 part 绕自身中心 `rot` 叠加，符合"以 parts 各自中心旋转"）、**[|]/[一] 镜像**（坐标绕联合中心镜像 + rot 取反）；框内空白拖动 = 整体移动
- **像素级验证**：5 项渲染/交互测试 PASS——矢量缩放中间态像素、程序元素 presize 缩放、Ctrl+点击 part 多选增删、part 多选青框 + 移动/旋转/缩放/镜像、元素 Ctrl+点击多选回归
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.6 补二 —— 多选可视化 + 图元全选（Ctrl+A）+ 元素整体旋转
- **① 多选画布可视化**：Ctrl 多选元素后，**每个成员画清晰黄色虚线框**（无手柄，一眼看到谁被选中），联合框保留（群组手柄：旋转/镜像/缩放）——此前只有联合框，成员不醒目
- **② 图元全选 Ctrl+A**：**[编辑] 模式（partsOpenEl）下 Ctrl+A 全选该元素所有图元**（`state.partMulti = {el, indexes[]}`）→ 每个选中图元画青色虚线框 + 动画面板批量应用（同元素多选语义，`animTarget` 多选分支覆盖）；非编辑态 Ctrl+A 仍为全选元素；ESC 退出图元多选
- **③ 黄框旋转改为元素整体旋转**：旋转手柄拖拽不再逐图元改 `part.rot`，改为写**元素级 `e.rot`**（图片/矢量/程序元素统一）——渲染端 `totalRot = e.rot + wave` **绕元素包围盒中心整体旋转**（drawParts/drawPartsTool 同步；wave 元素级语义不变）；旧 per-part `rotPartsCoords` 不再被拖拽调用
- **④ 修复**：`scrollOx` 引用已删除变量 → 移动拖拽失效（上一轮已修，本轮复测通过）
- **像素级回归**：默认配置（e.rot=0、无 wave）下 train/signal 与原版 **DIFF=0**；元素级旋转/图元多选/批量 bob 渲染测试 PASS
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.6 补 —— 滚动角度完整方向 + 群组操作 + 程序元素复制
- **② scrollAngle 完整方向**：`scroll.angle` 定义完整滚动方向（0=右，90=下坠落，180=左）；**瓦片沿同方向排列**（斜向无缝）；旧 `dir` 字段兼容（无 angle 时 left→180°/right→0°）；移除「滚动方向」与「动画类型」两个冗余下拉行（方向并入 angle，动画由左侧[动画调整]面板统一管理，旧配置兼容）
- **③ 多选群组操作**：Ctrl 多选后画**联合包围盒 + 群组手柄**——四角等比缩放（围绕对角锚点，各元素位置/尺寸/parts 同步）、旋转 ●（各元素绕联合中心旋转 + 自身 rot 叠加）、[|]/[一] 镜像（各元素镜像）；**动画面板批量应用**（多选时设置应用到全部成员）
- **④ 程序元素复制**：选中程序元素（selProg）Ctrl+C → 复制为 **images 素材元素**（含 parts/anim/scroll 完整克隆，脱离顶层键），粘贴为新 images 元素
- **① 修复**：`scrollOffsets` 重构遗漏——pointerdown 移动拖拽的 `scrollOx` 引用已删除变量 → 图片/矢量元素**无法拖动移动**（缩放/旋转/镜像正常，与现象吻合）；已修复
- **像素级回归**：默认配置（无 angle）下 train/signal 与原版 **DIFF=0**
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.6 —— wave 摆旋 + bob 斜向 + 滚动角度（scrollAngle）
- **新动画原语 `wave`（摆旋）**：与 bob 参数格式平行——`amp`（度，默认 10）+ `period`（秒，默认 1）→ 绕中心来回摆动（`rot = amp×sin(t/per×2π)`，非单向旋转）；**元素级 = 绕元素包围盒中心**、**part 级 = 叠加到该 part 的 rot**（绕自身中心）；应用顺序：bob（平移）→ wave（旋转）→ pulse（缩放）→ blink（透明度）
- **bob 加 `angle` 参数**：偏移方向（度，0=垂直，90=水平）——`xOff = sin(angle)×phase, yOff = cos(angle)×phase`；默认 0 = 原垂直行为
- **滚动角度 scrollAngle**：素材 `scroll.angle`（度，0=水平，90=垂直下落，45=斜向雨/流星整组）——`scrollOffsets` 位移向量分解：`ox = dir×off×cos(angle)`（保留 left/right 水平语义），`oy = off×sin(angle)`（恒正，angle>0 向下落）；瓦片沿 x 排列、整体 y 跟随（首尾衔接为斜向近似）；工具命中检测/选中框同步跟随 oy；imgForm 滚动参数行新增「滚动角度」输入
- **工具端滚动偏移重构**：`scrollOffset`（单值）→ `scrollOffsets`（{x,y}），全部调用点（渲染/平铺/命中/选中框/bbox）适配
- **像素级回归**：默认参数（无 wave/angle/scrollAngle）下 train/signal 与原版 **DIFF=0**；单元 4 项 PASS（wave 相位、bob 斜向分解、scrollAngle 垂直/水平、元素 wave 渲染）
- SCENE_SPEC：动画原语表更新（wave/bob angle）+ 滚动斜向说明 + **平铺元素 partsBand 拆解规范**（单元几何提取/几何变体约定：多形状循环建议多帧素材或接受单形状，rail/fog 变体示例）
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.5 补 —— 三端选中同步 + 动画面板不跨对象
- **① 三端同步（canvas/左侧动画面板/图层栏）**：`syncSelUI` 统一接入所有选中变化路径（画布点选元素/图元、图层栏卡片点击、图元条目点击、空白清除）——选中变化时图层栏重建（高亮）+ 左侧动画面板实时刷新；**图层元素级卡片增加选中黄色描线**（`sel` 类，覆盖图片元素与程序元素 `selProg`）
- **② parts 级双向**：画布点击图元 → 图层栏跳转至对应 parts 条目 + 青框高亮（滚动定位）；点击图层栏 parts 条目 → 画布选中对应图元（青框）+ 动画面板更新
- **③ 修复动画面板状态继承**：面板数据每次按当前选中目标实时读取重建——切换目标后不再显示/应用上一个对象的动画状态（此前面板只在手动切标签时刷新，旧目标状态残留导致误应用）
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.5 —— 通用动画原语体系 + 左侧栏双状态
- **动画原语化（skill 暴露能力，同名无意义）**：trainFx/signalFx 等按元素名绑定的 fx **全部移除**，拆解为通用原语：
  - `anim` 字段可挂在**元素或任意 part** 上（标签式多动画叠加）：字符串简写 `'bob'`（旧兼容）/ 单对象 `{type, ...}` / 多对象 `{bob:{...}, blink:{...}}`
  - 原语枚举：`bob`（垂直轻震，amp/period 默认 1px·1/6s，与原 `floor(t*6)%2` 一致）/ `blink`（透明度方波，on/off/period/duty/phase——支持非对称占空与相位）/ `pulse`（缩放脉动）
  - **作用顺序文档化**：bob（y 偏移）→ pulse（缩放）→ blink（透明度），维度独立可叠加
- **parts 级动画**：信号灯 = 绿/红两重叠 part + **互补 blink**（`green {period:4000, duty:3/4, phase:1/4}` + `red {period:4000, duty:1/4, phase:0}`）；列车车窗灯 = 静态暗 part + 亮 part blink（`{period:2333, duty:6/7, phase:1/7}`）——任何元素可做"两态切换/周期点亮"
- **光束几何实体化**：`drawBeam` 通用（元素声明 `beam`/`beamLen`/`beamOrigin`/`beamSpread`），列车车头灯为其实例；非动画（多关键帧范畴，待关键帧系统）
- **像素级验证**：16 时间点（覆盖 bob 两相/灯亮灭/信号切换/雾幕光束/多场景）**DIFF=0**——动画参数化前后视觉完全一致
- **左侧栏双状态**：「素材导入 / 动画调整」顶部标签切换；动画面板按选中目标（优先图元[编辑]青框，其次元素黄框/卡片）显示 bob/blink/pulse 三原语开关+参数；选中图元的图层卡片对应部分高亮（青框）
- SCENE_SPEC：动画原语枚举/顺序/典型组合（两态切换/周期点亮/叠加）写入 §七，作为 skill 暴露给生成 agent 的能力
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.4 补 —— 填充/描边取消勾选颜色保留
- **[编辑] 图元面板填充/描边取消勾选后颜色不再丢失变黑**：此前取消勾选置 `fill/stroke=null` 后 `commit()` 重建表单，色板控件以 `|| '#000000'` 初始化 → 重新勾选变黑；现取消时原色保留到 `_fillColor`/`_strokeColor`（表单重建/重开 js 均可恢复），重新勾选还原原色；序列化保留该字段（随配置写回）
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.4 —— 动态元素 parts 化 + SCENE_SPEC Agent 规范（①）
- **train 列车 parts 化**：静态车身（车体/驾驶室/车厢/底盘/轮子/车窗/车头/车尾 = 17 个 rect）拆为 parts（坐标相对元素原点 x,y）；新增元素级动画 `anim:'bob'`（1px 垂直轻震，drawParts 内应用，与 fx 同步）；时间动画保留为**动态覆盖 fx**：车窗灯闪烁（`trainFx` 在 6 个窗位覆盖 lampLit/lampDim）+ 车头灯光束（beam 三角，长度按场景 beamLen）
- **signal 信号灯 parts 化**：杆/臂拆为 parts（相对 x,y=54）；绿/红灯切换保留为 `signalFx` 动态覆盖
- **渲染顺序修正**：train/signal 的 layer 回调绕过 elDraw（其「有 parts 即只画 parts」会吞掉动态 fx），改为 `drawParts → fx` 直接组合——这是本轮的**关键 bug 修复**（此前 fx 永不执行，灯/光束丢失）
- **像素级验证**：拆解前后三帧（t=0/0.55/1.2，覆盖 bob 两相/灯亮灭/雾幕光束）渲染 **DIFF=0**——视觉完全一致；旧配置（无 parts）走原全量绘制，向后兼容
- **SCENE_SPEC §七「动态元素 parts 化」**：Agent 拆解规则——静态几何 → parts（逐像素一致）；时间动画 → 保留代码 fx（渲染端内建，Agent 无需生成）；元素级动画字段 `anim`（blink/pulse/bob）；复杂关键帧动画建议 images 多帧素材；工具内建能力补充 pixelDiv/alphaMode
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.3 补四 —— 四角缩放锚点修复 + 多边形手柄 + 单击自动切回选择
- **修复四角缩放锚点反了**：partresize（子图元青框）与 resize（元素黄框，parts 元素）的缩放锚点此前**锚的是拖动角自身**而非对角——拖 br 角时图形向左上跑偏（椭圆等表现为"无法缩放/位置乱跳"）；现锚定对角（拖 br 锚 tl / 拖 bl 锚 tr / 拖 tr 锚 bl / 拖 tl 锚 br），位置与尺寸同时正确
- **多边形（poly）四角缩放 + 旋转手柄**：此前 poly 的选中框不显示缩放手柄/旋转 ●（`pp.type !== 'poly'` 排除）；现放开——poly 顶点缩放（`scalePartAround` 已支持）与旋转（`partCenter` 顶点包围盒中心）可用
- **单击未创建图形自动切回[选择]**：矩形/直线/椭圆模式下单击（位移 <3px）不生成图元后，绘制状态自动变回[选择]（抽出 `setDrawMode` 统一工具栏高亮/提示/状态）；拖动（真正绘制）保持当前绘制模式并提交
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.3 补三 —— 边缘透明像素一步到位（完全消除半透明）
- **简化语义（固定 128 分界，撤销阈值可调）**：
  - **消除（remove）**：`alpha<128 → 透明` + `alpha>=128 → 不透明`——**一步到位只剩 {0,255}**（边缘所有半透明像素被二分，彻底消除毛刺）
  - **增强（boost）**：所有半透明像素（alpha>0）→ 不透明（透明像素保持）——同样只剩 {0,255}
  - **保留（keep）**：默认软边（现状零变化）
- 顺带修复：阈值控件读取 `|| 128` 把合法值 0 吞成 128 的隐患（控件已移除，不复发）
- 元素级（`applyAlphaMode` 离屏后处理）与素材级（`pixelate` 循环内）语义一致；home-scene 渲染端同步

## v2.3 补二 —— 像素化除数覆盖矢量元素 + 边缘透明像素处理
- **④ 像素化除数覆盖绘制生成的矢量元素**：绘制生成的矢量元素属 images 层、卡片走 `imgForm`（此前只有程序元素走 `walkForm` 才有此参数）——抽取共用 `pixelDivControl`，两类卡片均渲染「像素化除数」
- **⑤ 边缘透明像素下拉（128 分界温和版）**：重采样（pixelDiv 离屏 / 素材采样）后椭圆/斜线边缘的半透明抗锯齿像素可按需处理——**保留**（默认，软边）/ **消除**（alpha<128 → 透明，淡毛刺去掉）/ **增强**（alpha>128 → 不透明，边缘填实更"硬"）；置于**采样除数右侧（左侧栏）+ 像素化除数右侧（元素卡片）**，素材级（`s.alphaMode`）与元素级（`el.alphaMode`）双端
- 实现：`pixelate` 循环内 alphaMode（素材采样）；`applyAlphaMode` 对元素级离屏 canvas 后处理；离屏画**不透明版**（元素级 alpha/动画 alpha 移到最近邻放大时应用，避免与边缘透明处理冲突——blink/pulse 动画不受影响）
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.3 补 —— 添加尺寸上限 + 等比缩放 + 清理
- **添加素材默认尺寸上限**：元素显示尺寸最大边 ≤ 画布**较小边的一半**（如 320×118 → 最大边 59），保持原比例；小素材不缩放；用户可再拉伸（点采样语义不受影响）
- **四角缩放默认等比**：拖动选中框角点默认**保持宽高比**（以主拖动方向比例为准，另一维跟随）；**Shift 拖角 = 自由缩放**（微调）
- **移除「应用选中素材」按钮**：样式化参数实时生效（开关/除数/色板/量化/去背景），按钮冗余
- 「像素化除数」确认渲染于**程序元素卡片**（含 parts 的元素，卡片展开后可见；位图素材走左侧样式化，无此行）

## v2.3 —— 素材样式化回左侧栏 + 采样除数 + 元素级重采样
- **样式化控件回左侧栏（独立四行）**：样式化开关 + 采样除数 / 色板 / 量化 / 去背景（顶部样式化区移除——它只作用于位图素材，矢量 part 另有元素级方案，见下）
- **采样除数替代采样宽**：采样尺寸 = 基图 ÷ 除数（1/1、1/2、1/4、1/8）——像素化语义统一（缩小 N 倍采样）；**样式化开关**（替代"应用选中素材"按钮）：关 = 原样 1:1 显示（不受样式化影响），开 = 按除数采样；参数修改实时生效（画布引用元素同步）
- **多帧帧边界修复（④）**：多帧基图 = 每帧最近邻缩到**第一帧原尺寸**的干净 sheet（帧宽导入时固定）；切帧恒用基图帧宽（`s.img.width / frames`），与采样宽解耦——改除数不再错位/串帧，调回原除数即可复原
- **元素级重采样（②，pixelDiv）**：程序元素卡片新增「像素化除数」（1/1 平滑 / 1/2 / 1/4）——parts 渲染到「包围盒 ÷ 除数」离屏小画布再最近邻放大回显示区域（椭圆等矢量边缘出锯齿感）；`drawPartsTool`/`drawParts` 双端支持；band/滚动平铺元素暂不生效（文档注明）；默认无字段 = 1 = 现有行为零变化
- **⑤ 画布/图层栏选中 ↔ 素材库双向同步**：画布点选/图层栏卡片选中含 `srcId` 的元素 → 左侧素材库高亮 + 样式化控件显示该素材参数（避免"画布选中了、样式化却作用于另一个素材"的误解）
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.2 —— 素材模型重构（样式化滤镜 + 点采样 + 缓存修复）
- **素材 = 原图 + 参数快照**：`source` 存原图 `img` + `{采样宽w, 色板palette, 量化quant, 去背景bgtol, frames}`——原图始终保留（风格化/恢复的依据）；采样结果（`getSourceCanvas`/`getSourceDataUrl`）惰性缓存，参数变更 `invalidateSource` 重建
- **元素引用素材（srcId）**：`el.srcId` 指向素材 + 独立显示尺寸 `w/h`（默认 = 采样尺寸，靠拉伸）——改素材参数（重采样/换色板）画布引用元素**实时同步**（渲染按 srcId 取新采样），不再需要逐元素更新 src
- **点采样渲染**：素材原图 → 按参数降采样到采样尺寸 → **最近邻放大到显示尺寸**（`imageSmoothingEnabled=false`）——元素拉大仍保持像素化风格（每采样像素成块），小图细节不丢
- **顶部「素材样式化」区**：采样宽/色板（PICO-8/GB/VGA）/量化/去背景 + **原图|采样效果并排预览**——滑块实时重采样选中素材；色板/量化/去背景语义见控件 tooltip；左侧栏精简为导入 + 素材库
- **保存硬编码冻结**：保存 js / 装配导出时 `el.src` 冻结为**当前采样 dataUrl**（素材参数快照不入配置，`srcId`/`_asset` 序列化剥除）；重采样需求回到素材库做
- **缓存修复**：
  - home-scene `imgCache` 键 `name` → **`src`**（同名不同素材不再串图）；新增 `HomeScene.clearImageCache()` 入口
  - 渲染优先元素 `_asset`（同步 canvas，无 Image 异步竞态；旧路径 `_img`/`imgCache[src]` 兼容）
  - **初始化缓存失效判定**：打开工具/载入 js/重像素化时按「画布元素实际引用的素材」同步 `_asset`，无引用缓存清空；素材库原图+参数不受清理影响
  - `ensureImage` 竞态占位保留兜底（删除素材回退 `el.src` dataUrl 时加载）
- **多帧素材**：基图 = 每帧最近邻缩到采样尺寸的干净 sheet（不预风格化），`getSourceCanvas` 逐帧应用色板/量化/去背景后横向拼接——帧结构保持（f1/f2 各自采样正确）
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.1 —— 选中框统一（旋转/镜像/翻转）
- **黄框旋转手柄修复（图片元素）**：此前图片元素黄框的旋转手柄命中在「框内命中」条件之内，而手柄位于包围盒上方 → 永远点不到；现旋转手柄/镜像按钮**先于框内命中**（图片与矢量元素共用），图片元素旋转写入 `e.rot`（度）
- **图片元素旋转/翻转**：渲染端 `drawOneImage`/`drawElement` 支持 `e.rot`/`e.flipH`/`e.flipV`（canvas transform 绕图片自身中心，旋转+翻转组合 = `flip·R(rot) = R(-rot)·flip`）；图片元素黄框旋转手柄、镜像按钮现全部可用
- **镜像新语义（flipH/flipV 参数 + rot 取反）**：
  - 图元级（青色框）：`flipH`/`flipV` 翻转参数切换 + `rot` 取反——渲染端绕图元自身中心翻转（替换旧「坐标围绕自身包围盒中心翻转」：带旋转的对称图形镜像角度正确）
  - 元素级（黄框）：图元元素 = parts 坐标绕**元素包围盒中心**镜像 + 各 part `rot` 取反；图片元素 = `flipH`/`flipV` + `e.rot` 取反
- **选中框镜像按钮**：旋转 ● 上方 `[|]`（垂直镜像）与包围盒中心右侧 `[一]`（水平镜像），7×7 深底方框 + 同色符号；[编辑] 面板「水平镜像/垂直镜像」按钮移除（「旋转复位」保留）
- **统一选中框（drawSelBox）**：虚线包围盒 + 四角手柄 + 旋转 ● + 镜像按钮 一套绘制逻辑，图片元素/程序元素/矢量元素/子图元共用（子图元仅颜色青色 `#4dc3ff` 不同，黄 `#ffd23f`）；带元素（partsBand）不显示手柄与镜像按钮
- **修复**：part 青色框 `[|]` 按钮与旋转 ● 命中区相邻（半径 10 vs 间距 9）→ 镜像判定**先于**旋转手柄，上沿点击不再误触旋转
- **v2.1 二轮修正（镜像旋转顺序 / 手柄缩小 / bg 序列同步）**：
  - **镜像对旋转角度生效**：渲染端变换顺序改为**先翻转后旋转**（`translate → rotate → scale`，视觉 = `flip·R(rot) = R(-rot)·flip`）——此前顺序相反导致「镜像只作用于原图、旋转方向不变」（旋转 47° 的图片镜像后仍呈 +47° 而非 -47°）；图片元素（`drawElement`/`drawOneImage`）与图元（`drawPartsTool`/`drawParts`）四个渲染器统一修正；像素级验证：flipH+rot=-90 渲染 = rot=90 渲染的镜像（直角顶点镜像位置正确）
  - **变换中心修正（partCenter）**：矩形/椭圆 = 中心、线段 = 中点、多边形 = 顶点包围盒中心——此前多边形/线段绕 `(x+0.5)` 旋转（偏移中心）
  - **图元填充颜色修复**：rect/ellipse/poly 填充分支此前未设置 `ctx.fillStyle`（用上一绘制颜色——装配模式下面板/背景色串色）——现 `if (q.fill) { ctx.fillStyle = q.fill; ctx.fill(); }` 统一（rect 直接 fillRect）
  - **手柄尺寸缩小 1/2（向上取整）**：四角手柄 7×7 → **4×4**、旋转 ● 半径 4 → **2**（命中区 10 → 5）、镜像按钮 7×7 → **4×4**（命中 ±4 → ±2）、四角命中阈值 12 → 6——像素级验证：角 4×4、按钮约 6×6（含描边 AA）、旋转 ● 宽 6（旧 9）
  - **bg 背景色随场景增减同步**：`syncSceneArrays` 此前只修程序元素/素材内部数组，**顶层 `bg` 数组漏掉**——新增场景后 `bg` 长度不增 → 渲染端 `bg[min(part, len-1)]` 与最后场景共色；现 `fix(cfg)` 覆盖顶层数组（新增=沿用末位补齐；删除=按被删索引 `splice` 移除槽位，剩余场景各自取值对齐，如删场景 1 → `bg` 移除槽 1）；已确认渲染端 `CFG.bg` 为**按场景顺序取用的数组**（`bg[part]`，短数组兜底取末位）
- 渲染端同步：`public/home-scene.js` → `tools/home-scene.js`（vendored）一致

## v2.0 —— 图元层（parts）+ 矢量绘制（v2 主线第一步）
- **黄框双击进编辑**：选择模式选中元素（图片/程序）后双击画布 → 自动进入 [编辑]（openEdit 复用按钮逻辑：暂停/锁定绘制目标/展开卡片滚动）
- **四角缩放**：元素/子图元选择框改**四角手柄**（去掉单点）——四角皆可拖，锚定对角缩放（图元按对角锚点 scalePartAround；图像元素按角调 x/y/w/h）
- **旋转手柄**：part 选中框顶部旋转手柄 → `part.rot`（度），渲染端 drawParts/drawPartsTool 逐图元 canvas transform 旋转（围绕自身中心）；程序元素/矢量元素整体旋转 = 全部 parts 同角
- **镜像按钮**：[编辑] 面板「水平镜像/垂直镜像/旋转复位」——翻转选中图元坐标（围绕自身包围盒中心）
- **Ctrl 多选**：Ctrl+点击切换元素多选（`state.multi`），多选后：拖动=批量移动、Delete=批量删除、Ctrl+C/V=批量复制/粘贴；画布显示多选黄色虚线框
- **修复**：选择模式不再误触子图元（此前图片循环仍做 part 命中，导致 parts 元素点选即进子图元、Ctrl 多选失效）；[编辑] 模式旋转手柄命中（手柄在包围盒上方）
- AI 式描边/填充（前述）等 v2.0 能力见下

## v1.6 —— 图层栏跟随时间轴 + 单值参数编辑 + 显示时间弹窗（逐场景显隐）
- **AI 式描边/填充**：part 字段改为 `{fill: 色|null, stroke: 色|null, strokeWidth}`（legacy `color/fill:bool` 自动归一）——绘制工具栏改「填（色）/描（色+启用开关）/线（线宽）」；[编辑] partForm 逐图元「填充（色+无）·描边（色+无+线宽）·坐标」
- **图元级坐标拖拽**：选择模式命中单个 part（矩形/椭圆/线段距离/多边形）→ 青色虚线高亮 → 拖动平移；元素缩放 = parts 按比例缩放（起点副本无累积误差）
- **程序元素叠加**：新增 parts 到程序元素（stars/clouds/…）自动 `partsMode='overlay'`（原绘制 + 图元叠加，不替代）；图片元素 = 图片 + parts 叠加；moon（迁移 parts）保持替代模式
- **单编辑锁定**：同时只能开启一个 [编辑]（`partsOpenEl` 唯一）；开启后**矢量绘制目标自动锁定为该元素**（目标选择器失效），+图元/画布绘制均落到该元素
- **热键系统**：空格=播放/暂停；Delete=删除选中图元/元素；Ctrl+Z=撤销（**5 步历史**，覆盖元素/图元/参数/场景/显隐/拖动等写入）；Ctrl+C/V/X=复制/粘贴/剪切（元素与图元两级剪贴板）
- **暂停态即时刷新**：删除/复制/添加素材等操作改走 `refreshStage()`（含 HomeScene.seek），暂停时画布同步更新
- **修复**：
  - **moon 图元颜色丢失/继承 stars**：根因 `normPart` 只判 `'fill' in p`——legacy `fill:true` **布尔占用 fill 键**未转换 → 渲染 `ctx.fillStyle = true`（非法）→ 继承上一图层（stars）填充色；修复：`typeof p.fill === 'boolean'` 先行转换（true→fill=color / false→stroke=color），渲染与 [编辑] partForm 统一
  - **程序元素 part 无法画布选中**：part 命中此前只遍历 `state.elements`（素材层）——新增程序元素（HOME_SCENE 顶层键）的 parts 命中（moon 等迁移/叠加图元可选中拖动）
  - **非 moon/train 元素 +图元无效**：根因 `elVal` 对缺失 x/y 返回 `undefined` → `part.x -= undefined = NaN`（signal 有 x 无 y → y=NaN；stars/clouds 等无坐标 → 双 NaN）；修复：`elVal`/`val` 缺失键按 **0**（图元以画布绝对坐标落位、随元素 scroll 偏移滚动），所有程序元素均可加图元
  - **进入 [编辑] 自动暂停**：开启图元面板时若正在播放则自动暂停（冻结移动带作为图元定位锚点）；图元随元素滚动**平铺**语义保持不变（决策确认）
  - **选择模式误改子图元**：子图元命中仅限 [编辑] 模式（partsOpenEl）；选择模式点击 parts 元素 = **整体选中**——程序元素新增 `selProg`（parts 包围盒虚线框 + 图层栏跳卡片），拖动 = 整体移动（有 x/y 改坐标（场景数组改当前槽）/ 无 x/y 平移全部 parts）
  - **带元素图元不跟随滚动**：根因两套滚动相位公式（程序带 `wrap(t*speed,span)` vs 图元 `(t*speed)%span`）相位不匹配 → 图元漂移；修复：**带相对模型**——speed/span 带元素（顶层或 scroll 字段）加图元时 `partsBand=true`，part.x 存**瓦片相位偏移**（`wrap(画布X + 当前offset, span)`），渲染按母带同款 wrap 公式逐瓦片（`j*span - offset + relX`）随带同步滚动不漂移；y 保持画布绝对（带为水平）
  - **选择模式整体选中不展开卡片**：selProg 命中时补 `layerOpen[k]=true` + 重建（与图片元素选中一致，卡片展开）
  - **带判定规则改为 x/y 任一缺失即触发**：`isBandEl` 由「两个都缺」改为「任一缺失」（作为持久化状态 partsBand，考虑后续 speed/span 自定义）——clouds（有 y 无 x + speed/span）等现可触发带相对模型；`bandSpeed` 兼容按场景数组 speed
  - **子图元缩放手柄**：part 选中（青色虚线框）右下角补**青色手柄**（与 part 选择框同色）——拖动按比例缩放 parts（复用 scalePart，起点副本无累积误差；带元素/多边形暂不支持手柄，可经 [编辑] 面板改坐标）
  - **ESC 分层退出**：有绘制中形状 → 取消绘制；选中子图元 → 清蓝框（保留元素黄框）；选中元素（黄框）→ 清选择并顺带退出 [编辑] 模式；仅 [编辑] 模式 → 退出编辑
  - **绘制单击不生成**：矩形/直线/椭圆指针抬起时若位移 <3px（单击）则不生成图元——只有拖动才生成，防误触
  - **Alt+滚轮缩放以鼠标为中心**：新增 `state.pan` 平移量——缩放时重算 pan 使**光标下的画布坐标保持不变**（此前固定左上角放大、局部无法放大）；双击复位同时清平移；尺寸变化重置平移
  - **图元独立透明度**：part 新增 `alpha`（0~1，与元素级相乘），[编辑] partForm 补「透明度」行；渲染端 drawParts/drawPartsTool 按图元级 × 元素级合成
  - **色板点色即关闭**：颜色 input 的 `input` 事件此前触发 DOM 重建（buildLayerList）→ 原生拾色器被替换关闭；改为 input 只刷新画布、不重建 DOM（拾色器可停留拖动），change 才记历史
- **包围盒归一化**：新建图元元素 x/y/w/h = parts bbox（选中框框住图形）；[编辑] 点击自动展开卡片并滚动定位
- 说明：动态图元（如信号灯时间变色）暂不支持（parts 静态色），后续关键帧扩展；**旋转手柄/镜像按钮/Ctrl 多选**见 PLAN（待实现）

## v1.6 —— 图层栏跟随时间轴 + 单值参数编辑 + 显示时间弹窗（逐场景显隐）
- **图层栏跟随时间轴**：多少套 scene 就有多少套图层栏状态——每行参数**只显示当前场景的一个值**（播放头所在段，`tlScene()`）；播放头跨场景/点击场景 chip（=切编辑场景+跳播放头）自动重建图层栏与高亮当前 chip；移除 v1.5 的多格按场景数组输入与 [S]/[常量] 大按钮
- **参数行小 [s] 开关**（默认关）：关=改动写常量（作用于所有场景）；开=改动只写**当前场景槽**（自动生成/维持按场景数组）；数字/颜色/文本参数统一支持；状态按「卡片|参数路径」记忆
- **显示时间弹窗**（卡片头 [S]，位于 [隐藏] 左侧）：逐场景显隐开关 + **场景内多段窗口条**（PS 渐变条式）——每场景一行轨道：**点击段=切分为两段 / 点击空白=加段（10% 居中）/ 拖段体=移动 / 拖两端手柄=缩放 / 双击段=删除**（220ms 防双击误切）；**窗口绝对秒数标注映射到时间轴**（弹窗元素窗口边界红色小字，0.1s 精度，[S] 面板保持干净）；**场景边界拖拽时显示边界绝对秒数**（红色 `24.0s` 于边界处，0.1s 精度，随拖拽实时更新）；内部存 0~1 比例（场景边界调整不破坏显示）
- **show 统一为按场景窗口数组**：`show: [null | [[f0,f1],…], …]`（null=隐藏；单窗口 `[f0,f1]` 自动归一为列表；f 为场景内 0~1 比例）；legacy 格式自动迁移（`{scenes:[..]}`/`[t0,t1]`/`hidden` 布尔 → 窗口数组，`migrateShow`）；[隐藏] 按钮 = 全局隐藏/恢复快捷（写入 show 全 null / 删除）
- **修复**：
  - **[S] 时间轴联动跨面板混显**：新增 `showFocusEl`——时间轴**只映射当前/最近打开的 [S] 面板元素**（色块 + 窗口秒标注），打开另一个面板时旧元素标注消失，不再多色混显；未打开过任何 [S] 时仍显示全部元素
  - **moon 场景1不可见**：原 home-scene 硬编码 `moon(part) { if (part === 1) return; }`（雾幕阶段不画月亮）无配置可调——已参数化：移除硬编码，默认配置补 `moon.show: [[[0,1]], null, [[0,1]], [[0,1]]]`（默认视觉不变，[S] 弹窗可调）
  - **全局高宽修改裁剪/黑边**：根因是 home-scene 画布在 `init()` 时固定 320×118，改 cfg.w/h 后与 stage 画布（已跟随）尺寸错位；新增 `HomeScene.resize()`（后备存储 = CFG.w×CFG.h，与 stage 对齐），gw/gh 修改与载入场景时调用——画面按背景色铺满新尺寸，无黑边、无裁剪
  - **重导入尺寸不生效**：`resize()` 读模块级 `CFG`，重导入时 `window.HOME_SCENE` 换新对象但 `CFG` 要等下一帧 `syncCfg` 才更新 → resize 用了旧值（面板显示 320×150、画布仍是 118）——修复：`resize()` 开头先 `syncCfg()`，重导入即时生效（DIAG 验证 canvas=320×150）
  - **初始不预设场景**：工具启动不再显示 home-scene 默认动画（`#home-scene` 默认隐藏、场景层勾选默认关、`showScene=false`）——载入实际 js 后场景层才显示对应画面并适应视窗
  - **显示框高度跟随 js 宽高比（封顶）**：`.stage-wrap` 高度由 JS 计算 = 容器宽 × cfg.h/cfg.w（载入即占满显示框），**上限 = 视口高 × 2/3**（`resizeStageWrap`）——高比例 js 不会吞掉底部时间轴/场景栏；gw/gh 修改、载入/装配、窗口 resize 时重算
  - **画布内容缩放（Alt+滚轮）**：`state.zoom`（0.2~8×，Alt+滚轮步进 ×1.1，双击画布复位）；双画布改为 JS 显式尺寸（fit × zoom，`applyCanvasDisplay`），`image-rendering: pixelated` 像素化缩放——zoomed 超出显示框部分被容器裁剪（`overflow: hidden`），右上角黄色缩放徽标（×1.1，双击复位）；`stagePos` 指针映射基于内容框
  - **固定显示框 + 等比缩放**：`.stage-wrap` 改为固定尺寸（宽 100% + 高 218px，**不再被 cfg.h 撑开**），双画布 `object-fit: contain; object-position: left top`——画布内容按 cfg.w:cfg.h 等比缩放 fit：动画整体缩小而非裁剪；比例比默认更高 → **右侧留空**（坐标定位下的新空间）；更宽 → 底部留空；`stagePos` 指针映射同步改为 contain 换算
- **修复**：bar 点击无响应——默认整段 `[0,1]` 占满轨道使「点空白加段」永远落在段内被吞掉；改为**点击段=切分**保证任何点击都有响应；另修 `normShowWindows` 无 show 时各场景共享同一数组引用的隐患
- **渲染端**：home-scene `elShown` 支持窗口数组（按场景边界取场景内时间），`sceneBounds` 辅助；工具时间轴 `showRanges` 按窗口逐段画色块
- **生成规范简化**（SCENE_SPEC v2）：Agent 生成只需**常量参数** + show 窗口数组（或省略=全程）；按场景数组由工具按需生成；legacy 兼容保留
- **修复**：
  - **[隐藏] 独立为「调试隐藏」**：只切换元素 `hidden`（全局覆盖，**不触碰 show 窗口**——避免"恢复后 scene 全激活"丢信息）；按钮随状态显示 隐藏/显示；保存时仍隐藏则写最外层 `hidden:true`（装配模式 `buildExportText` 同步补 hidden 字段）；`migrateShow` 不再折叠 hidden
  - **[S] 弹窗真正影响画布**：根因是 home-scene 图层表仅 fog/signal/bridge 尊重 `elShown`——现**所有程序元素**统一包裹 `elShown` 检查（show 窗口对每个元素生效）
  - **暂停时修改不同步**：新增 `refreshStage()`（renderStage + renderTimeline + `HomeScene.seek(t)` 强制场景层重绘一帧），walkForm/imgForm 全部编辑、[隐藏]、[S] 弹窗、画布拖拽结束（pointerup）均调用——暂停态改动立即可见
  - **导入识别场景专属参数**：数值/颜色**按场景数组在导入时自动激活 [s]**（`rowS` 初始化），显示当前场景值且编辑只写当前槽，不破坏数组；常量参数 [s] 默认置灰
- **背景跟随场景 + 移入图层栏顶部**：画布顶部工具栏的「背景」移除，改为**图层栏顶部单独成行的「背景 bg」卡片**——跟随播放头所在场景显示/编辑该场景背景色（`cfg.bg` 按场景数组），右侧 **[s] 开关**（数组默认开=只改当前场景槽，关=所有场景同色；装配模式为单一背景 `state.asmBg`）；home-scene 渲染支持 `bg` 常量或数组（`Array.isArray` 分支）
- **自包含兜底（独立仓库铺路）**：`tools/home-scene.js` 内置副本（与游戏 `public/home-scene.js` 手动同步）；`img2asset.html` 优先加载 `../public`、缺失自动回退本地副本并手动 `HomeScene.init()`；home-scene.js `init` 幂等保护——工具可脱离游戏仓库独立运行（为拆分 CanvasCartoonBuilder 仓库铺路，已验证独立环境正常渲染）
- 场景 chip 样式区分：active=实线黄框，`+ 场景`=虚线置灰（不再误读为激活）

## v1.5 —— 参数轨道 + 场景内建编辑 + 生成规范（概念层落地）
- **参数轨道模型**（替代按场景散键）：数值/颜色参数三态——常量 number ↔ **按场景数组** `[v0,v1,…]`（长度=场景数）↔ 关键帧 `{t,v}`（预留 PLAN P1）。渲染端统一取值 `val(e,k,t)`/`valAt(e,k,part)`（常量→自身；数组→按 scene(t) 取，长度不足取末位）；home-scene.js 全量迁移（moon 的 `x/x3/y/y2/y3`、clouds 的 `speed1/color1`、mountains 的 `speed2/peak2/color3/fill2a/fill2b`、train 的 `beamLenFog/beamFog` 等 → 按场景数组），**视觉与行为不变**
- **场景内建编辑**：`scenes` 声明后，画布与时间轴之间出现**场景栏**——场景 chip 点击改名 / ✕ 删除 / `+ 场景` 新增（等分插入）；**时间轴边界拖拽**（顶部三角手柄，±6px 命中，写 `sceneBorders`，非均匀段正确显示）；loop 调整联动钳制边界；场景数变化自动**补齐/截断所有按场景数组**（`syncSceneArrays`）；`show:{scenes}` 区间按实际边界计算
- **按场景数组编辑**：walkForm/imgForm 中长度=场景数的数值/颜色/rgba 数组 → **每场景一个输入框**（title 标注场景名）+ [常量] 合并按钮；常量行带 **[S] 拆分按钮**（一键转按场景数组）
- **生成规范**：新增 `tools/SCENE_SPEC.md`（顶层结构/元素 schema/参数类型表/七条约定/反例正例）；工具打开 js 时**规范自检**（`checkSceneSpec`）——按场景散键（`x3` 且存在 `x`）、数值数组长度≠场景数（只读）、`show:{scenes}` 未声明 `scenes`，提示于 m-stage/m-props
- **动态场景判定**：home-scene `scene(t)` 优先 `sceneBorders` 否则按 scenes 数等分（兜底 4 段）；loop 每帧读 `CFG.loop`（此前 `LOOP` 常量在加载时固化，改 loop 不生效的隐患一并修复）
- **拖拽重排 = 换 z + 换卡片顺序**（v1.4 延续）：程序元素按键顺序 in-place 重插、素材按新序 splice，卡片顺序与 z 一致并随保存写回
- 修复：`delElement`/`dupElement` 重复定义清理（v1.4 遗留）

## v1.4 —— 界面重构：全局上移 / 右侧纯图层栏 / 删导出区 / 时间轴标准化
- **布局**：场景文件按钮（打开 js / 保存 js）与全局设置合并为画布**顶部工具栏**（sf-row），右侧面板只剩**图层栏**（元素卡片）——全局参数 = js 全局配置，归位到顶部；**字段名保留**（宽/高/周期/背景 逐一标注，一行排列）
- **保存 js 移至图层栏右上角**（props-head 头部右侧）；图层栏高度**占满整个页面**（`.p-props` 纵向 flex + `#cfgform` flex:1）
- **画布点击联动**：点击画布可编辑元素 → 图层栏**自动滚动定位**到对应卡片并展开（`focusLayerCard` + `scrollIntoView`）
- **卡片头部重构**：**z 值与展开箭头移到左侧**（⠿ z值 ▾ 名称……），右侧新增**[隐藏/显示] 与 [删除] 按钮**（所有元素卡片，含程序化元素）——隐藏置灰卡片（`.layer-card.hid`）、装配/场景层均跳过绘制（home-scene.js 图层过滤 `hidden`）；程序元素删除需确认（保存后场景不再绘制），且 home-scene.js 图层表对已删除元素容错（`CFG.x && {...}` 守卫）
- **删导出区**：移除「生成导出代码 / 复制全部」UI 与使用说明（页面内改写已替代复制粘贴）；`buildExportText` 保留，仅供「保存 js」装配模式落盘
- **素材参数改为卡片展开项**：不再常驻属性面板——素材卡片**点击选中即展开**（再点收起），展开项含全部参数（含 scroll/anim/show）+ 复制；程序化元素卡片点击展开调参；画布点击元素同样触发选中展开
- **时间轴通用化 + 保留 SCENE 划分**：默认/无 scenes 的 js 为**通用中性轨道**（浅色底 + 均匀刻度 + 秒数 + 红色播放头 + show 时段彩色块，非像素风，适合作为通用工具的初始态）；打开含 `scenes:["段名",...]` 的 js（home-scene.js 已补 `scenes` 字段）后**保留场景段划分**——段底色 + 段名标注；`show:{scenes:[..]}` 的段长按 `loop/段数` 计算；高度降至 16px
- **素材卡片选中高亮**（`.layer-card.sel` 边框）；空场景显示空态提示
- **时间轴清晰化**：画布按「CSS 尺寸 × supersample(≥2 或 devicePixelRatio)」建后备存储 + `setTransform` 按 CSS 像素绘制——拉伸/高分屏不再插值放大发虚；高度 20px、刻度字 9px、段名字 9px（红色播放头）
- **图层栏内部滚动**：`#cfgform` 改 `flex:1 1 0 + height:0`（基准 0，不再被内容撑开）+ 显式滚动条样式（`::-webkit-scrollbar`/`scrollbar-width`）——展开多个卡片时**页面高度不变**，列表内部滚动
- **素材卡片表单中文说明（回调上一版本）**：滚动速度（像素/秒）/平铺周期/滚动方向（向左·向右）、动画类型（**静态/闪烁/脉动** 下拉）、动画周期（毫秒）、帧数/帧率（多帧说明）、显示时间（**空=全程 · 0,2=场景段 · 5-20=秒区间**，可编辑回写 `show`）——替换通用 walkForm 的裸字段
- **拖拽重排 = 换 z + 换卡片顺序**：`reorderLayers` 在重排 z 的同时**物理重排卡片**——程序元素按键顺序 in-place 重插（保持 cfg 对象引用，保存写回新顺序）、素材按新序 splice 原数组（装配模式 state.elements 同数组随之更新）；卡片上下顺序与 z 一致
- 顶部工具栏压缩为**单行**（nowrap + 紧凑间距 + 字段名标注），[播放/暂停] `margin-left:auto` 靠右
- 修复：`escapeHtml` 缺失导致图层栏渲染抛错（卡片名转义补全）；`delElement`/`dupElement` 重复定义清理

## v1.3 —— 图层列表 + 时间轴 + 多帧素材
- **PS 式图层列表**：参数区改为全局卡片（w/h/loop/bg）+ 元素卡片——程序化元素常态收起、点击展开调参；素材卡片点击=选中（右侧面板调参）；**拖拽卡片排序 → 自动重排 z**（0,1,2…）
- **时间轴**（画布底部，0~48s）：4 段场景刻度（夜原/雾/山口/桥）+ 段名/秒数；**点击/拖动 scrub 跳转**（`HomeScene.seek(t)`）；素材/元素 `show` 时段显示为彩色块
- **场景条件元素参数化**：fog/signal/bridge 的出场条件改为配置 `show`（默认 `{scenes:[1]/[2]/[3]}`），表单/时间轴可调——绑定场景（`0,2`）与绑定时间（`5-20`）均支持
- **滚动 span 补全**：属性面板新增 span（平铺周期）输入（此前缺失）
- **多帧素材（sprite sheet）**：多图导入按序合成横向帧表 + `frames`/`fps` 参数（属性面板可调）；工具渲染、home-scene、导出渲染器统一支持按帧裁切绘制；GIF 解码列入 PLAN
- 导出渲染器同步支持 `show` 显示条件与多帧裁切；保存链路验证（z/show/frames 写回再导入成功）

## v1.2 —— 页面内修改闭环（打开/保存 js）+ home-scene 参数化
- **打开 js**：读取场景文件，提取 `SCENE`（装配器格式）或 `HOME_SCENE`/`DEFAULT_HOME_SCENE`（参数化场景格式）——平衡括号提取 + JS 字面量求值解析
- **装配器 SCENE 载入**：还原画布尺寸/背景/元素（含素材），可直接拖拽/调参
- **场景参数载入**：配置注入 `window.HOME_SCENE` → **通用参数表单自动生成**（数字/颜色/文本/布尔/颜色数组递归遍历）→ 调整**实时生效**于场景层动画
- **保存 js**：File System Access API（localhost/https）写回原文件，环境不支持则下载替换——目标页面刷新即生效，全程页面内完成
- **修复**：home-scene.js 配置改为**每帧同步**（`let CFG` + draw 开头 `syncCfg()`）——此前 `const CFG` 在脚本加载时一次性求值，工具调参不生效、重开新 js 也显示旧画面；注入验证通过（列车位置/远山颜色/云速度实时变化）
- **UI**：参数表单固定容器高度（max-height 280px + 滚动），不再挤占下方元素面板
- **素材自动写入场景**：`HOME_SCENE` 新增 `images` 叠加层（home-scene 渲染它）——场景参数模式下左侧导入的素材以**同一引用**写入 `window.HOME_SCENE.images`（场景层实时渲染 + 保存随配置写回）；保存序列化用 replacer 剥除内部 `_img` 字段；注入验证通过（素材浮于场景之上）
- **图层顺序（z）**：程序化元素与素材统一按 `z` 排序渲染（`DEFAULT_HOME_SCENE` 每元素含 z，参数表单可直接编辑；素材默认 99 浮于最上）——验证：z=2 素材被 z=4 远山遮住
- **素材显示时间（show）**：属性面板「显示时间」——空=全程 / `0,2`=场景段 / `5-20`=秒区间；home-scene `imgShown()` 按场景或时间窗口控制素材显隐——验证：show 场景段生效
- **暂停/播放联动**：工具播放键同时控制素材层循环与场景层（`HomeScene.start/stop`）
- **保存自检 + 错误可见化**：保存前用提取逻辑自检写回文本（防写坏文件）；解析失败显示具体原因（替换"未找到"笼统提示）
- 依赖：`public/home-scene.js` 已参数化（元素参数集中 `DEFAULT_HOME_SCENE` + `images` 素材叠加层，运行时优先 `window.HOME_SCENE`，动态效果保留代码、视觉不变）

## v1.1 —— 场景层预览 + 画布尺寸跟随
- **场景层**：工具画布底部叠加 `../public/home-scene.js` 实时动画（夜行列车 48s 四段循环，独立层），素材浮于其上预览整体效果；场景层加载失败有提示
- **尺寸跟随**：home-scene.js 硬编码 320×118 → 场景层开启时画布锁定该尺寸（输入禁用）；关闭场景层可自定义画布尺寸与纯色背景
- 素材层画布透明（`clearRect`），透出底层场景层

## v1.0 —— 像素风小动画装配器（初始版）
- **像素化管线**：任意图片 → 缩放目标像素（关闭平滑）→ 减色（PICO-8 32 色 / Game Boy 4 色 / VGA 16 色色板，或均匀量化滑杆）→ 纯色去背景（左上角色 + 容差）
- **可视化装配**：场景画布（默认 320×118）拖拽移动 / 拖角缩放 / 点击选中；属性面板（x/y/w/h/透明度/层级/滚动速度方向/动画类型/周期）
- **滚动与动画**：滚动元素按 span 无缝平铺（速度/方向）；blink 透明度闪烁、pulse 大小脉动
- **自包含导出**：SCENE 配置（base64 素材内嵌）+ 通用渲染器 + 自动播放，粘贴任意网页即可运行；已用实际渲染验证
- **工程**：`tools/` 目录独立于游戏本体（`.dockerignore` 排除），工具改动独立提交
